apiVersion: apps/v1
kind: Deployment
metadata:
  name: export-service
  namespace: kavachio
  labels: { app: export-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: export-service }
  template:
    metadata:
      labels: { app: export-service }
    spec:
      containers:
        - name: export-service
          image: kavachio/export-service:latest
          ports:
            - containerPort: 8005
          envFrom:
            - secretRef: { name: kavachio-secrets }
          env:
            - name: SERVICE_PORT
              value: "8005"
          readinessProbe:
            httpGet: { path: /health/db, port: 8005 }
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet: { path: /health, port: 8005 }
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
---
apiVersion: v1
kind: Service
metadata:
  name: export-service
  namespace: kavachio
spec:
  selector: { app: export-service }
  ports:
    - port: 8005
      targetPort: 8005
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: export-service
  namespace: kavachio
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: export-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
