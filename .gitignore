# macOS
.DS_Store

# Node
node_modules/
UI/
# Python
__pycache__/
*.pyc
venv/
.venv/
docs/
Demo/

# Azurite (local Azure Blob Storage emulator) data
.azurite/
backend/python-services/.azurite/

# Env files
.env
backend/python-services/.venv/*
backend/js-services/dist/*
backend/js-services/package-lock.json
frontend/node_modules/
frontend/dist/
# Local env overrides (per-developer)
.env.local
*.env.local 
ai_call_trace/

# graphify knowledge-graph output (regenerated locally, no API cost)
graphify-out/
# SFTP intake landing zone (local dev). Broker files, never committed.
backend/python-services/sftp-root/

# TypeScript incremental build cache — generated on every build, so it
# conflicts on every pull if it is tracked.
frontend/tsconfig.tsbuildinfo
*.tsbuildinfo
